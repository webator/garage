    <nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
        <div class="container-fluid">
            <a class="navbar-brand col-4" href="/admin"><img src="/images/logo.png" width="150" /></a>
            <div class="col-12 col-md-4 right">
                 <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse " id="navbarColor01">
                    <ul class="navbar-nav me-auto">      
                        {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link" href="/admin/users">Utilisateurs</a>
                        </li>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') %}
                        <li>
                            <a class="nav-link" href="/admin/services">Services</a>
                        </li>
                        {% endif %}
                        {% if is_granted('ROLE_EMPLOYE') %}
                        <li>
                            <a class="nav-link" href="/admin/occasion">Occasions</a>
                        </li>
                        {% endif %}
                        {% if is_granted('ROLE_EMPLOYE') %}
                        <li>
                            <a class="nav-link" href="/admin/temoignages">Témoignages</a>
                        </li>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') %}
                        <li>
                            <a class="nav-link" href="/admin/horaires">Horaire</a>
                        </li>
                        {% endif %}
                        <li>
                            <a class="nav-link" href="/admin/logout">Déconnexion</a>
                        </li>

                    </ul>
                </div>
            </div>
        </div>
    </nav>